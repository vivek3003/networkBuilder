# Network Builder: A project with Redux, React and D3

To Setup just clone the repository.

To install Dependencies:
`npm install`

For Development:
`npm run dev`

For Build:
`npm run build`

For Tests:
`npm run test`